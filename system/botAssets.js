import config from "../config.js";

export const BOT_NAME =
  config.BOT_NAME || "𝐓𝐇𝐄_𝐏𝐀𝐈𝐍-MD";

export const DEV =
  config.OWNER_NAME || "𝐓𝐇𝐄 𝐏𝐀𝐈𝐍";

export const CHANNEL =
  config.CHANNEL || "";

export const IMAGES = {

  menu:
    config.MENU_IMAGE || "",

  support:
    config.SUPPORT_IMAGE || "",

  botinfo:
    config.BOTINFO_IMAGE || "",

  owner:
    config.OWNER_IMAGE || "",

  connect:
    config.CONNECT_IMAGE || ""

};


export const AUDIOS = {

  menu:
    config.MENU_AUDIO || "",

  connect:
    config.CONNECT_AUDIO || ""

};