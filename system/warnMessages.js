export const WARN_MESSAGES = {

  PRIVATE_MODE:
    "☠️ THE PAIN SYSTEM IS PRIVATE",

  OWNER_ONLY:
    cmd =>
      `☠️ ONLY OWNER CAN USE .${cmd}`,

  ADMIN_ONLY:
    cmd =>
      `☠️ ONLY ADMINS CAN USE .${cmd}`
};